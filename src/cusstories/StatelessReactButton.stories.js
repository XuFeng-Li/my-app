
import React from 'react';

import {StatelessReactButton} from './StatelessReactButton';

export default {
    title:'Example/StatelessReactButton',
    component:StatelessReactButton,
};

const Template = () => <StatelessReactButton />;

export const StatelessButton = Template.bind();
